const filterCurrencyResponse = () => {
    data.filter((curr) => {
        //     if (curr.type_is_crypto === 1 && curr.price_usd)
        //         return curr;
        // });
}
export { filterCurrencyResponse }